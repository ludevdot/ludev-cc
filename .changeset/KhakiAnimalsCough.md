---
"ludev-cc": patch
---

improve commands from real-world feedback: changeset validates existing changesets and bump types, split-task reads files before splitting, release-tag auto-consumes changesets and detects Vercel/Netlify, find-the-bug adds session handling and recovery commands
