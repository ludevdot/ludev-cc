---
"ludev-cc": minor
---

- add interactive mode with command selector and install target choice
- add `remove` and `update` subcommands
- add `--global` flag for all subcommands
- add `--help` flag
- add 25 tests with Node.js native test runner
